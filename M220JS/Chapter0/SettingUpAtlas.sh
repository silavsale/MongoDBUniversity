# If you need to run this course using a local instance of MongoDB, you can download the sample data from here and then import it using the ``mongorestore`` command:

mongorestore --gzip --archive=sampledata.archive.gz